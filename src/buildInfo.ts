export const BUILD_VERSION = '4.3.2';
export const BUILD_DATE = '2026-07-01 06:00 IST';
export const BUILD_NOTE = 'Added animated splash screen: three candlestick bars from the logo appear one at a time (left→middle→right, 320ms apart), then the screen fades out to reveal the app (~1.4s total). Renders as a position:absolute overlay so providers and the navigator load in parallel underneath. Removed from the tree entirely after onDone() fires.';
